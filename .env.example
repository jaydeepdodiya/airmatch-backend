# Server
PORT=3000
NODE_ENV=development

# Local dev: skip Firebase token verification (uses demo-user)
SKIP_AUTH=true

# Production: path to Firebase service account JSON
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
